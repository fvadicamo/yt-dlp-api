# Core dependencies
fastapi==0.141.1
# starlette arrives through fastapi, which declares it as `starlette>=0.46.0`
# with no ceiling, so an unpinned install takes the newest release in existence
# and CI can go red on a PR that changed nothing. Pinned here for the same
# reason everything else in this file is: the version is an input, not a
# result. Bumps come from dependabot like any other dependency.
starlette==1.4.0
uvicorn[standard]==0.51.0
pydantic==2.13.4
pydantic-settings==2.14.2

# Logging
structlog==26.1.0

# Monitoring
prometheus-client==0.23.1
psutil==7.2.2

# Configuration
pyyaml==6.0.3

# HTTP client (webhook delivery)
httpx==0.28.1

# Caching
cachetools==7.1.4

# yt-dlp is pinned separately in requirements-ytdlp.txt (used by the
# Docker build and refreshed weekly by the docker-publish workflow)
